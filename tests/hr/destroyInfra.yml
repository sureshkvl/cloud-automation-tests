---
- name: "Delete the Test Environment"
  hosts: localhost
  gather_facts: false
  become: true
  connection: local

  tasks:
  - include_tasks: destroystack.yml
    register: task_result

  - debug:
      var: task_result    
